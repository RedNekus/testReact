import styled, { css } from 'styled-components'
import { maxWidth, minWidth, rgba } from '@/assets/styles/mixins.css.js'
import vars from '@/assets/styles/vars'

const Test = styled.div``;

Test.Body = styled.div``;

export default Test